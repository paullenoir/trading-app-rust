# NeuralTradingFrontend

This project was generated using [Angular CLI](https://github.com/angular/angular-cli) version 21.1.1.

## Development server

To start a local development server, run:

```bash
ng serve
```

Once the server is running, open your browser and navigate to `http://localhost:4200/`. The application will automatically reload whenever you modify any of the source files.

## Code scaffolding

Angular CLI includes powerful code scaffolding tools. To generate a new component, run:

```bash
ng generate component component-name
```

For a complete list of available schematics (such as `components`, `directives`, or `pipes`), run:

```bash
ng generate --help
```

## Building

To build the project run:

```bash
ng build
```

This will compile your project and store the build artifacts in the `dist/` directory. By default, the production build optimizes your application for performance and speed.

## Running unit tests

To execute unit tests with the [Vitest](https://vitest.dev/) test runner, use the following command:

```bash
ng test
```

## Running end-to-end tests

For end-to-end (e2e) testing, run:

```bash
ng e2e
```

Angular CLI does not come with an end-to-end testing framework by default. You can choose one that suits your needs.

## Additional Resources

For more information on using the Angular CLI, including detailed command references, visit the [Angular CLI Overview and Command Reference](https://angular.dev/tools/cli) page.


Dossiers principaux
/src : Code source de l'application

/app : Composants, services, modules (votre code)
/assets : Images, fonts, fichiers statiques
index.html : Point d'entrée HTML
main.ts : Point d'entrée TypeScript (bootstrap l'app)
styles.css : Styles globaux

/public : Fichiers publics servis tels quels (moins utilisé maintenant, remplacé par /assets)
Fichiers .json :

package.json : Dépendances npm, scripts
tsconfig.json : Configuration TypeScript
angular.json : Configuration Angular (build, serve, chemins)

# Méthodologie complète de développement d'un composant Angular

| Étape | Explication | Code exemple |
|-------|-------------|--------------|
| **1. Bâtir le DOM** 🏗️ | Créer la structure HTML du template avec éléments de base, contenu statique et données mockées pour visualiser | ```html<br><!-- trade-form.component.html --><br><div class="trade-form-container"><br>  <form><br>    <input type="text" placeholder="Symbole" /><br>    <input type="number" placeholder="Quantité" /><br>    <button type="submit">Créer Trade</button><br>  </form><br></div><br>``` |
| **2. Refactorer le DOM pour le SEO** 🔍 | Remplacer par balises sémantiques HTML5, ajouter attributs ARIA, labels, optimiser hiérarchie titres et accessibilité | ```html<br><section class="trade-form-container" aria-label="Formulaire de création de trade"><br>  <h2>Nouveau Trade</h2><br>  <form role="form" aria-labelledby="trade-form-title"><br>    <label for="symbol">Symbole</label><br>    <input id="symbol" type="text" aria-required="true" /><br>    <br>    <label for="quantity">Quantité</label><br>    <input id="quantity" type="number" aria-required="true" /><br>    <br>    <button type="submit" aria-label="Créer le trade">Créer Trade</button><br>  </form><br></section><br>``` |
| **3. Faire le service** ⚙️ | Créer service TypeScript pour logique métier, appels API avec HttpClient, gestion observables, lier composant au service avec binding dynamique | ```typescript<br>// /src/app/services/trade.service.ts<br>/**<br> * Service de gestion des trades<br> * Dépendances: HttpClient<br> * Endpoints: POST /api/trades<br> */<br>import { Injectable } from '@angular/core';<br>import { HttpClient } from '@angular/common/http';<br>import { Observable } from 'rxjs';<br><br>@Injectable({ providedIn: 'root' })<br>export class TradeService {<br>  private apiUrl = 'http://localhost:5000/api/trades';<br><br>  constructor(private http: HttpClient) {}<br><br>  /**<br>   * Crée un nouveau trade<br>   * @param trade - Objet contenant symbole, quantité, prix<br>   * @returns Observable du trade créé<br>   */<br>  createTrade(trade: any): Observable<any> {<br>    return this.http.post(this.apiUrl, trade);<br>  }<br>}<br>```<br><br>```typescript<br>// /src/app/components/trade-form/trade-form.component.ts<br>/**<br> * Composant formulaire de création de trade<br> * Dépendances: TradeService, FormBuilder<br> */<br>import { Component } from '@angular/core';<br>import { FormBuilder, FormGroup, Validators } from '@angular/forms';<br>import { TradeService } from '../../services/trade.service';<br><br>@Component({<br>  selector: 'app-trade-form',<br>  templateUrl: './trade-form.component.html',<br>  styleUrls: ['./trade-form.component.scss']<br>})<br>export class TradeFormComponent {<br>  tradeForm: FormGroup;<br>  loading = false;<br>  error: string \| null = null;<br><br>  constructor(<br>    private fb: FormBuilder,<br>    private tradeService: TradeService<br>  ) {<br>    this.tradeForm = this.fb.group({<br>      symbol: ['', [Validators.required]],<br>      quantity: [0, [Validators.required, Validators.min(1)]]<br>    });<br>  }<br><br>  onSubmit(): void {<br>    if (this.tradeForm.valid) {<br>      this.loading = true;<br>      this.tradeService.createTrade(this.tradeForm.value).subscribe({<br>        next: (response) => {<br>          console.log('Trade créé:', response);<br>          this.tradeForm.reset();<br>          this.loading = false;<br>        },<br>        error: (err) => {<br>          this.error = 'Erreur lors de la création du trade';<br>          this.loading = false;<br>        }<br>      });<br>    }<br>  }<br>}<br>```<br><br>```html<br><!-- Template dynamique --><br><section class="trade-form-container"><br>  <h2>Nouveau Trade</h2><br>  <form [formGroup]="tradeForm" (ngSubmit)="onSubmit()"><br>    <label for="symbol">Symbole</label><br>    <input id="symbol" formControlName="symbol" type="text" /><br>    <br>    <label for="quantity">Quantité</label><br>    <input id="quantity" formControlName="quantity" type="number" /><br>    <br>    <button type="submit" [disabled]="!tradeForm.valid \|\| loading"><br>      {{ loading ? 'Création...' : 'Créer Trade' }}<br>    </button><br>    <br>    <p *ngIf="error" class="error">{{ error }}</p><br>  </form><br></section><br>``` |
| **4. Validation et gestion des erreurs** ⚠️ | Validation côté client avec Reactive Forms, messages d'erreur utilisateur, gestion erreurs API, feedback visuel pour champs invalides | ```typescript<br>// Validation personnalisée<br>this.tradeForm = this.fb.group({<br>  symbol: ['', [<br>    Validators.required, <br>    Validators.pattern(/^[A-Z]{1,5}$/)<br>  ]],<br>  quantity: [0, [<br>    Validators.required, <br>    Validators.min(1), <br>    Validators.max(10000)<br>  ]],<br>  price: [0, [<br>    Validators.required, <br>    Validators.min(0.01)<br>  ]]<br>});<br>```<br><br>```html<br><input formControlName="symbol" /><br><div *ngIf="tradeForm.get('symbol')?.invalid && tradeForm.get('symbol')?.touched"><br>  <p class="error" *ngIf="tradeForm.get('symbol')?.errors?.['required']"><br>    Symbole requis<br>  </p><br>  <p class="error" *ngIf="tradeForm.get('symbol')?.errors?.['pattern']"><br>    Format invalide (1-5 lettres majuscules)<br>  </p><br></div><br>``` |
| **5. États de chargement** ⏳ | Spinners pendant appels API, skeleton screens, désactivation boutons pendant envoi, messages de chargement, gestion états vides | ```html<br><div *ngIf="loading" class="spinner"></div><br><div *ngIf="!loading && trades.length === 0"><br>  Aucun trade disponible<br></div><br><div *ngIf="!loading && trades.length > 0"><br>  <div *ngFor="let trade of trades"><br>    {{ trade.symbol }} - {{ trade.quantity }}<br>  </div><br></div><br>```<br><br>```typescript<br>// Dans le composant<br>loading = false;<br>trades: Trade[] = [];<br><br>loadTrades(): void {<br>  this.loading = true;<br>  this.tradeService.getTrades().subscribe({<br>    next: (data) => {<br>      this.trades = data;<br>      this.loading = false;<br>    },<br>    error: () => {<br>      this.loading = false;<br>    }<br>  });<br>}<br>``` |
| **6. Le CSS** 🎨 | Créer styles dans component.scss, appliquer couleurs/typographies/espacements, ajouter états hover/focus/disabled, animations et transitions | ```scss<br>// /src/app/components/trade-form/trade-form.component.scss<br>.trade-form-container {<br>  max-width: 500px;<br>  margin: 2rem auto;<br>  padding: 2rem;<br>  background: #ffffff;<br>  border-radius: 8px;<br>  box-shadow: 0 2px 8px rgba(0,0,0,0.1);<br><br>  h2 {<br>    margin-bottom: 1.5rem;<br>    color: #333;<br>    font-size: 1.5rem;<br>  }<br><br>  form {<br>    display: flex;<br>    flex-direction: column;<br>    gap: 1rem;<br><br>    label {<br>      font-weight: 600;<br>      color: #555;<br>      margin-bottom: 0.25rem;<br>    }<br><br>    input {<br>      padding: 0.75rem;<br>      border: 1px solid #ddd;<br>      border-radius: 4px;<br>      font-size: 1rem;<br>      transition: border-color 0.3s;<br><br>      &:focus {<br>        outline: none;<br>        border-color: #007bff;<br>      }<br>    }<br><br>    button {<br>      padding: 0.75rem;<br>      background: #007bff;<br>      color: white;<br>      border: none;<br>      border-radius: 4px;<br>      font-size: 1rem;<br>      cursor: pointer;<br>      transition: background 0.3s;<br><br>      &:hover:not(:disabled) {<br>        background: #0056b3;<br>      }<br><br>      &:disabled {<br>        background: #ccc;<br>        cursor: not-allowed;<br>      }<br>    }<br><br>    .error {<br>      color: #dc3545;<br>      font-size: 0.9rem;<br>    }<br>  }<br>}<br>``` |
| **7. Responsive** 📱 | Media queries CSS pour différentes tailles, adapter layouts avec Flexbox/Grid, tester mobile/tablet/desktop, approche mobile-first | ```scss<br>// Responsive<br>@media (max-width: 768px) {<br>  .trade-form-container {<br>    margin: 1rem;<br>    padding: 1rem;<br><br>    h2 {<br>      font-size: 1.25rem;<br>    }<br><br>    form {<br>      input, button {<br>        font-size: 0.9rem;<br>      }<br>    }<br>  }<br>}<br><br>@media (max-width: 480px) {<br>  .trade-form-container {<br>    padding: 0.75rem;<br><br>    form {<br>      gap: 0.75rem;<br>    }<br>  }<br>}<br>``` |
| **8. Sécurité** 🔒 | Sanitisation inputs (XSS), protection CSRF, authentification/autorisation avec guards, validation serveur, headers sécurisés | ```typescript<br>// /src/app/guards/auth.guard.ts<br>/**<br> * Guard de protection des routes authentifiées<br> * Redirige vers /login si non authentifié<br> */<br>import { Injectable } from '@angular/core';<br>import { CanActivate, Router } from '@angular/router';<br>import { AuthService } from '../services/auth.service';<br><br>@Injectable({ providedIn: 'root' })<br>export class AuthGuard implements CanActivate {<br>  constructor(<br>    private authService: AuthService,<br>    private router: Router<br>  ) {}<br><br>  canActivate(): boolean {<br>    if (this.authService.isAuthenticated()) {<br>      return true;<br>    }<br>    this.router.navigate(['/login']);<br>    return false;<br>  }<br>}<br>```<br><br>```typescript<br>// Dans routing module<br>const routes: Routes = [<br>  { <br>    path: 'trades', <br>    component: TradeListComponent,<br>    canActivate: [AuthGuard]  // Protection<br>  }<br>];<br>``` |
| **9. Performance et optimisation** ⚡ | Lazy loading modules, OnPush change detection, TrackBy pour *ngFor, unsubscribe observables, debounce recherches, virtual scrolling | ```typescript<br>// OnPush pour performance<br>import { ChangeDetectionStrategy } from '@angular/core';<br><br>@Component({<br>  selector: 'app-trade-list',<br>  changeDetection: ChangeDetectionStrategy.OnPush<br>})<br>export class TradeListComponent {<br>  // TrackBy pour *ngFor<br>  trackByTradeId(index: number, trade: Trade): number {<br>    return trade.id;<br>  }<br>}<br>```<br><br>```html<br><div *ngFor="let trade of trades; trackBy: trackByTradeId"><br>  {{ trade.symbol }} - {{ trade.quantity }}<br></div><br>```<br><br>```typescript<br>// Lazy loading dans routing<br>const routes: Routes = [<br>  {<br>    path: 'trades',<br>    loadChildren: () => import('./trades/trades.module')<br>      .then(m => m.TradesModule)<br>  }<br>];<br>``` |
| **10. Tests unitaires et E2E** ✅ | Tests unitaires composants (.spec.ts), tests services avec mock HttpClient, tests E2E avec Cypress/Playwright, coverage de code | ```typescript<br>// trade-form.component.spec.ts<br>/**<br> * Tests unitaires du composant TradeForm<br> * Teste: création de trade, validation, gestion erreurs<br> */<br>import { ComponentFixture, TestBed } from '@angular/core/testing';<br>import { TradeFormComponent } from './trade-form.component';<br>import { TradeService } from '../../services/trade.service';<br>import { of, throwError } from 'rxjs';<br><br>describe('TradeFormComponent', () => {<br>  let component: TradeFormComponent;<br>  let fixture: ComponentFixture<TradeFormComponent>;<br>  let tradeService: jasmine.SpyObj<TradeService>;<br><br>  beforeEach(() => {<br>    const spy = jasmine.createSpyObj('TradeService', ['createTrade']);<br>    <br>    TestBed.configureTestingModule({<br>      declarations: [TradeFormComponent],<br>      providers: [{ provide: TradeService, useValue: spy }]<br>    });<br>    <br>    fixture = TestBed.createComponent(TradeFormComponent);<br>    component = fixture.componentInstance;<br>    tradeService = TestBed.inject(TradeService) as jasmine.SpyObj<TradeService>;<br>  });<br><br>  it('should create trade when form is valid', () => {<br>    tradeService.createTrade.and.returnValue(of({ id: 1 }));<br>    <br>    component.tradeForm.setValue({ <br>      symbol: 'AAPL', <br>      quantity: 10 <br>    });<br>    component.onSubmit();<br>    <br>    expect(tradeService.createTrade).toHaveBeenCalledWith({<br>      symbol: 'AAPL',<br>      quantity: 10<br>    });<br>  });<br><br>  it('should display error on API failure', () => {<br>    tradeService.createTrade.and.returnValue(<br>      throwError(() => new Error('API Error'))<br>    );<br>    <br>    component.tradeForm.setValue({ <br>      symbol: 'AAPL', <br>      quantity: 10 <br>    });<br>    component.onSubmit();<br>    <br>    expect(component.error).toBe('Erreur lors de la création du trade');<br>  });<br>});<br>``` |
| **11. Accessibilité avancée (a11y)** ♿ | Navigation clavier complète (Tab/Enter/Escape), focus management, lecteurs d'écran (ARIA), contraste couleurs WCAG, skip links | ```html<br><!-- Navigation clavier et focus --><br><div class="modal" role="dialog" aria-labelledby="modal-title"><br>  <h2 id="modal-title">Confirmation</h2><br>  <p>Êtes-vous sûr de vouloir supprimer ce trade ?</p><br>  <br>  <button <br>    (click)="confirmDelete()" <br>    (keydown.enter)="confirmDelete()"<br>    cdkFocusInitial<br>    aria-label="Confirmer la suppression"><br>    Confirmer<br>  </button><br>  <br>  <button <br>    (click)="closeModal()" <br>    (keydown.escape)="closeModal()"<br>    aria-label="Annuler et fermer la modale"><br>    Annuler<br>  </button><br></div><br>```<br><br>```scss<br>// Contraste couleurs WCAG AA<br>.button-primary {<br>  background: #0056b3; // Contraste 4.5:1 minimum<br>  color: #ffffff;<br>}<br><br>// Focus visible<br>button:focus-visible {<br>  outline: 3px solid #005fcc;<br>  outline-offset: 2px;<br>}<br>``` |
| **12. Documentation inline** 📝 | Commentaires en-tête fichier (rôle/dépendances), JSDoc pour fonctions/méthodes, documentation routes dans routing.module.ts | ```typescript<br>/**<br> * /src/app/components/trade-form/trade-form.component.ts<br> * <br> * Composant de gestion des trades<br> * Dépendances: TradeService, FormBuilder, Router<br> * Routes associées: /trades/new, /trades/:id/edit<br> * <br> * Fonctionnalités:<br> * - Création de nouveaux trades<br> * - Validation en temps réel<br> * - Gestion des erreurs API<br> * <br> * @example<br> * <app-trade-form [mode]="'create'"></app-trade-form><br> */<br>import { Component, Input } from '@angular/core';<br><br>@Component({<br>  selector: 'app-trade-form',<br>  templateUrl: './trade-form.component.html'<br>})<br>export class TradeFormComponent {<br>  /**<br>   * Mode du formulaire (create ou edit)<br>   * @default 'create'<br>   */<br>  @Input() mode: 'create' \| 'edit' = 'create';<br><br>  /**<br>   * Soumet le formulaire et crée/met à jour le trade<br>   * @returns void<br>   * @throws {Error} Si la validation échoue<br>   */<br>  onSubmit(): void {<br>    // Implementation<br>  }<br>}<br>```<br><br>```typescript<br>// /src/app/app-routing.module.ts<br>/**<br> * Configuration des routes principales<br> * <br> * Routes disponibles:<br> * - GET  / : Page d'accueil<br> * - GET  /trades : Liste des trades (protégé)<br> * - GET  /trades/new : Création trade (protégé)<br> * - GET  /trades/:id : Détail trade (protégé)<br> * - GET  /login : Page de connexion<br> */<br>const routes: Routes = [...];<br>``` |
| **13. Gestion d'état (State Management)** 🗄️ | NgRx/Akita pour apps complexes, services avec BehaviorSubject pour état partagé, LocalStorage/SessionStorage pour persistance | ```typescript<br>// /src/app/services/trade-state.service.ts<br>/**<br> * Service de gestion d'état global des trades<br> * Utilise BehaviorSubject pour état réactif partagé<br> */<br>import { Injectable } from '@angular/core';<br>import { BehaviorSubject, Observable } from 'rxjs';<br><br>export interface Trade {<br>  id: number;<br>  symbol: string;<br>  quantity: number;<br>  price: number;<br>}<br><br>@Injectable({ providedIn: 'root' })<br>export class TradeStateService {<br>  private tradesSubject = new BehaviorSubject<Trade[]>([]);<br>  public trades$: Observable<Trade[]> = this.tradesSubject.asObservable();<br><br>  /**<br>   * Ajoute un trade à l'état global<br>   * @param trade - Trade à ajouter<br>   */<br>  addTrade(trade: Trade): void {<br>    const current = this.tradesSubject.value;<br>    this.tradesSubject.next([...current, trade]);<br>    this.saveToLocalStorage();<br>  }<br><br>  /**<br>   * Supprime un trade de l'état<br>   * @param id - ID du trade à supprimer<br>   */<br>  removeTrade(id: number): void {<br>    const current = this.tradesSubject.value;<br>    this.tradesSubject.next(current.filter(t => t.id !== id));<br>    this.saveToLocalStorage();<br>  }<br><br>  /**<br>   * Sauvegarde l'état dans LocalStorage<br>   */<br>  private saveToLocalStorage(): void {<br>    localStorage.setItem('trades', JSON.stringify(this.tradesSubject.value));<br>  }<br><br>  /**<br>   * Charge l'état depuis LocalStorage<br>   */<br>  loadFromLocalStorage(): void {<br>    const saved = localStorage.getItem('trades');<br>    if (saved) {<br>      this.tradesSubject.next(JSON.parse(saved));<br>    }<br>  }<br>}<br>```<br><br>```typescript<br>// Utilisation dans composant<br>export class TradeListComponent implements OnInit {<br>  trades$: Observable<Trade[]>;<br><br>  constructor(private tradeState: TradeStateService) {<br>    this.trades$ = this.tradeState.trades$;<br>  }<br><br>  ngOnInit(): void {<br>    this.tradeState.loadFromLocalStorage();<br>  }<br>}<br>```<br><br>```html<br><div *ngFor="let trade of trades$ \| async"><br>  {{ trade.symbol }}<br></div><br>``` |
| **14. Internationalisation (i18n)** 🌍 | Support multi-langues avec Angular i18n ou ngx-translate, formats dates/nombres localisés, textes traduits | ```html<br><!-- Utilisation de i18n Angular --><br><h2 i18n="@@trade.form.title">Nouveau Trade</h2><br><br><label i18n="@@trade.form.symbol">Symbole</label><br><input type="text" placeholder="Ex: AAPL" i18n-placeholder="@@trade.form.symbol.placeholder" /><br><br><button i18n="@@trade.form.submit">Créer</button><br>```<br><br>```typescript<br>// Avec ngx-translate<br>import { TranslateService } from '@ngx-translate/core';<br><br>export class TradeFormComponent {<br>  constructor(private translate: TranslateService) {<br>    this.translate.setDefaultLang('fr');<br>    this.translate.use('fr');<br>  }<br><br>  switchLanguage(lang: string): void {<br>    this.translate.use(lang);<br>  }<br>}<br>```<br><br>```html<br><!-- Template avec ngx-translate --><br><h2>{{ 'TRADE.FORM.TITLE' \| translate }}</h2><br><label>{{ 'TRADE.FORM.SYMBOL' \| translate }}</label><br>```<br><br>```json<br>// /src/assets/i18n/fr.json<br>{<br>  "TRADE": {<br>    "FORM": {<br>      "TITLE": "Nouveau Trade",<br>      "SYMBOL": "Symbole",<br>      "SUBMIT": "Créer"<br>    }<br>  }<br>}<br>``` |
| **15. Analytics et monitoring** 📊 | Google Analytics ou Matomo, tracking événements utilisateur, error tracking (Sentry/Rollbar), performance monitoring | ```typescript<br>// /src/app/services/analytics.service.ts<br>/**<br> * Service de tracking analytics<br> * Intégration: Google Analytics 4<br> */<br>import { Injectable } from '@angular/core';<br><br>declare let gtag: Function;<br><br>@Injectable({ providedIn: 'root' })<br>export class AnalyticsService {<br>  /**<br>   * Track un événement utilisateur<br>   * @param eventName - Nom de l'événement<br>   * @param eventParams - Paramètres additionnels<br>   */<br>  trackEvent(eventName: string, eventParams: any = {}): void {<br>    if (typeof gtag !== 'undefined') {<br>      gtag('event', eventName, eventParams);<br>    }<br>  }<br><br>  /**<br>   * Track une page vue<br>   * @param pageUrl - URL de la page<br>   */<br>  trackPageView(pageUrl: string): void {<br>    if (typeof gtag !== 'undefined') {<br>      gtag('config', 'GA_MEASUREMENT_ID', {<br>        page_path: pageUrl<br>      });<br>    }<br>  }<br>}<br>```<br><br>```typescript<br>// Utilisation dans composant<br>export class TradeFormComponent {<br>  constructor(private analytics: AnalyticsService) {}<br><br>  onSubmit(): void {<br>    if (this.tradeForm.valid) {<br>      this.tradeService.createTrade(this.tradeForm.value).subscribe({<br>        next: (response) => {<br>          // Track succès<br>          this.analytics.trackEvent('trade_created', {<br>            symbol: this.tradeForm.value.symbol,<br>            quantity: this.tradeForm.value.quantity<br>          });<br>        },<br>        error: (err) => {<br>          // Track erreur<br>          this.analytics.trackEvent('trade_creation_failed', {<br>            error: err.message<br>          });<br>        }<br>      });<br>    }<br>  }<br>}<br>```<br><br>```typescript<br>// Error tracking avec Sentry<br>import * as Sentry from "@sentry/angular";<br><br>// Dans app.module.ts<br>Sentry.init({<br>  dsn: "YOUR_SENTRY_DSN",<br>  environment: "production"<br>});<br><br>// Auto-capture des erreurs Angular<br>@NgModule({<br>  providers: [<br>    {<br>      provide: ErrorHandler,<br>      useValue: Sentry.createErrorHandler()<br>    }<br>  ]<br>})<br>``` |

---

## Ordre pratique recommandé pour votre application de trading

**Phase 1 - Core Development** (Étapes 1-7) :
- DOM → SEO → Service → Validation → États → CSS → Responsive

**Phase 2 - Quality & Polish** (Étapes 8-12) :
- Sécurité → Performance → Tests → Accessibilité → Documentation

**Phase 3 - Advanced Features** (Étapes 13-15, selon besoins) :
- State Management → i18n → Analytics
